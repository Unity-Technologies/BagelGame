:root {
}

.b-screen {
    flex-direction: row;
    flex-grow: 1;
}

.b-pane {
    background-color: rgba(0, 0, 0, 0);
    transition-property: filter;
    transition-duration: 1s;
}

.b-pane:disabled {
    filter: blur(17.24);
}

.b-background-pane {
    position: absolute;
}
